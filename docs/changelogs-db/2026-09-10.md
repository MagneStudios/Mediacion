# Changelog — Proyecto Mediación (DB)

## 2026-09-10 — `planes.is_self_serve` (distinguir free, self-serve y "a consultar")

> Prompt `docs/prompts-db/2026-09-10-planes-is-self-serve.md`; decisión `docs/decisiones-db/2026-09-10-planes-self-serve.md`.

### Migración (total: 45)

| # | Migración | Tipo |
|---|---|---|
| 45 | `20260910120000_planes_is_self_serve.sql` | Aditiva (ADD COLUMN + UPDATE corporativo) |

- `is_self_serve BOOLEAN NOT NULL DEFAULT true` + `COMMENT`.
- `UPDATE planes SET is_self_serve = false WHERE nombre = 'corporativo'` — único plan "a consultar" (se contrata por ventas); `base` (0.00) y los de pago quedan `true`.
- Sin backfill, sin tocar RLS, sin `handle_new_user`, sin `precio` (sigue `NOT NULL`).

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `supabase/migrations/20260910120000_planes_is_self_serve.sql` | Nueva |
| `packages/db-types/src/database.types.ts` | `planes` Row/Insert/Update con `is_self_serve: boolean` (Insert/Update opcional por DEFAULT) |
| `docs/database.md` | Sección `planes` + árbol de migraciones (45) |

### QA

- `supabase db reset` **45/45**; `db lint` 0 errores.
- `SELECT nombre, is_self_serve FROM planes ORDER BY nombre` → `base=true, corporativo=false, estudio=true, particular=true, plus=true, simple=true`.
- `smoke_migrations.py` **92/92**; `validate_rls.py` **61/61** en run limpio (vía `setup_test_env.ps1`) — el criterio "10 fallos preexistentes" corresponde a entornos con seed TYC previo; este cambio no toca nada que valide RLS.
- `packages/db-types` typecheck **exit 0**.
- Sin cambios en `apps/api` ni `mediacion-app` (por regla del prompt).

### No degradar / contexto conocido

- Las 8 suites de integración de jest rojas por la migración 44 (INSERT guard) son preexistentes y fuera de este cambio (`docs/changelogs/2026-09-10-alta-de-negociaciones.md`).