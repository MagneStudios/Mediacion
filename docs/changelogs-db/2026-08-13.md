# Changelog — Proyecto Mediación

## 2026-08-13

### Contexto del revert

El PR #97 ("Revert 'Feat/supabase db'", commit `1c3cbe0`, mergeado como `225720e`) revirtió todo el trabajo DB de agosto (migración `20260810120000_cambios_reunion_07_08.sql`, las 3 migraciones de linter, el fix de API `plan-limit`, tipos, DBML, scripts y docs). La resolución de conflictos posterior (`8ea3d59`) mantuvo el revert en la rama, pero los mocks del frontend (R-04…R-10, commits `1ee44e9`…`5e6c9db`) sí quedaron: el frontend espera un schema (`pago_a_cargo`, `facturas`, plan estudio ilimitado, estado `expirado`) que la base ya no tenía.

**Decisión (registrada en `docs/decisiones-db/2026-08-13-decisiones-restauracion-revert.md`):** re-landear el trabajo revertido restaurando los archivos exactos del estado pre-revert (`1c3cbe0^` = `ef5e817`, merge PR #95), sin reescribir SQL ni cherry-pickear merges. Contenido ya validado en su momento (66/66 smoke, 17/17 validate_rls, 896 tests API, lint 0).

Commits de referencia originales: `a59cf2c` (cambios reunión 07/08), `391eea3` (corrección warnings linter), `2538f42` (fix plan-limit R-10).

**Commits de restauración (rama `feat/supabase-db`):**
- `a56b139` — `feat(db): restaura migraciones de agosto revertidas (R-04...R-12 + linter)`
- `0663df8` — `feat(db): regenera database.types y mediacion.dbml (R-04...R-12)`
- `c2fbfd3` — `fix(api): tratar limite_casos NULL como ilimitado (R-10)`
- `9b646d0` — `test(db): smoke 24 tablas + validate_rls 17 checks`
- `1e2ec4a` — `docs: restaura docs y changelogs DB de agosto post-revert`

### Migraciones restauradas

Total en `supabase/migrations/`: **23 → 27** (las 4 de agosto, sin modificar el resto).

| Migración | Contenido |
|-----------|-----------|
| `20260810120000_cambios_reunion_07_08.sql` | R-04 expirado + TTL 72 h, R-05 matrícula, R-07 `pago_a_cargo`, R-09 `facturas`, R-10 plan estudio ilimitado, R-12 `envios_email` |
| `20260811130000_linter_security_revoke.sql` | REVOKE EXECUTE triggers (PUBLIC/anon/authenticated) + helpers (PUBLIC/anon) + hardening `inversores_insert_anon` |
| `20260811140000_linter_perf_consolidate_policies.sql` | 1 policy SELECT por tabla (firmas, items, notificaciones, usuarios): 52 → 47 policies |
| `20260811150000_linter_perf_fk_indexes.sql` | 16 índices de cobertura en FKs |

- Verificado: sin duplicados estilo macOS; timestamps posteriores a la última migración existente (`20260730180000_backend_gap_features.sql`).
- No se restauró `docs/changelogs/2026-08-10.md` (versión del tree ya consolidada: DB + Frontend) ni nada del frontend (`mediacion-app/`).

### Linter y RLS

- `supabase db reset` local: **27/27 migraciones aplican sin error** (CLI 2.109.1, PostgreSQL 17.6).
- `supabase db lint`: **0 warnings**.
- Policies: **47 en 24 tablas** (post-consolidación, sin cambios).
- EXECUTE de helpers: revocado a `anon`/PUBLIC, conservado para `authenticated` + `service_role` (postura segura de `20260811130000`, vigente).

### Tests

| Suite | Resultado |
|-------|-----------|
| `pnpm typecheck` (raíz, tsc -b) | **PASS** |
| `pnpm --filter @mediacion/api test` | **896 passed, 0 failed** (57 skipped: 14 suites `describe.skip` que requieren `DATABASE_URL` real; `plan-limit.service.spec.ts` incluido y verde) |
| `supabase db reset` | 27/27 migraciones OK |
| `scripts/smoke_migrations.py` | **66/66 PASS** (24 tablas, incl. `facturas`/`envios_email`, 7 claves de `configuracion`) |
| `scripts/setup_test_env.ps1` + `scripts/validate_rls.py` | **17/17 PASS** |
| `supabase db lint` | **0 warnings** |

> Nota: en el primer run de `pnpm test` 5 specs E2E de controllers fallaron por timeout de 5000 ms (flaky conocido); al re-correr la suite completa pasó 896/0. No es un fallo de la restauración.

### Tipos y documentación

- `packages/db-types/src/database.types.ts`: +`facturas`/`envios_email`, +4 columnas en `usuarios`, `pago_a_cargo`, `limite_casos` nullable, enum `estado_caso` +`expirado`.
- `mediacion.dbml` regenerado (R-04…R-12 aplicados).
- `docs/database.md` restaurado: árbol con **27 migraciones**, 24 tablas.
- `docs/decisiones-db/2026-08-10-cambios-reunion.md`, `docs/plan-implementacion-07-08-2026.md`, `docs/Cambios_Reunion_Mediacion_07-08-2026.md`, `docs/changelogs/2026-08-11.md`, `docs/changelogs/2026-08-12.md` restaurados.
- `docs/mediacion_db_develop.md` (local, gitignored): corregida incoherencia interna — encabezado 52 → **47 policies en 24 tablas**; además se alinearon `validate_rls.py` 13 → 17 checks y el conteo de índices de la sección 8.2 (16 → 18).

### Pendientes

- **Push al VPS (Coolify, schema `public`)** — el remoto tiene las 22 tablas viejas; aplicar el delta de las 4 migraciones según `docs/db-push-remoto.md` (`supabase db push` con CLI como contenedor Docker en la red del stack). **Requiere backup previo de la DB remota y OK explícito del equipo.**
- **Deuda de seguridad detectada en sesión:** secrets trackeados en git (`vps_key.pem`, `backup.sql`, `solo_public.sql`) — depuración de historial como tarea aparte (ver `docs/decisiones-db/2026-08-13-decisiones-restauracion-revert.md`).
- **Backend de R-09/R-12:** la restauración toca solo schema, tipos y `plan-limit`; la lógica de facturación/emails sigue esperando su ficha de backend.
