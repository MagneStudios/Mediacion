# Changelog — Proyecto Mediación

## 2026-08-18

> Archivo creado de antemano (18/08/2026). Los tres prompts de `docs/prompts-db/` de esta fecha documentan acá sus resultados en las secciones marcadas.

### Respuestas PM de la auditoría (decisiones)

- ✅ Documentadas en `docs/decisiones-db/2026-08-18-respuestas-pm-auditoria-db.md`: 8 respuestas (Q-2…Q-6, N-1, N-3, A-3/A-4/R-1/R-2). Ninguna requirió migración nueva. Seis hallazgos de la auditoría referenciaban objetos inexistentes en el repo (`20260806180000`, `20260815000000`, `pagos_stripe`, `notify_change_of_state()`, `cambios_caso`, `items_fijos_por_negociacion`).

### DBML

- ✅ Sincronizado `mediacion.dbml` con el schema post-auditoría (30 tablas).
- **Agregado:** `rate_limit_counters` (PK compuesta `(clave, ventana_inicio)`, columna `hits`, índice `idx_rate_limit_counters_ventana`, nota de diseño: tabla de rol de servidor).
- **Verificado sin drift:** `avisos_version_legal` y `solicitudes_contacto` coinciden exactamente con `20260815120000_legal_avisos_contacto.sql` (columnas, FKs, índices, notas).
- **FKs agregadas:** `avisos_version_legal.usuario_id → auth.users [RESTRICT]` y `solicitudes_contacto.usuario_id → auth.users [SET NULL]` a la sección de relaciones legales.
- **Enums verificados:** `estado_arrepentimiento` (4 valores) y `estado_caso` (8 valores) consistentes con las migraciones.
- No se encontraron conflictos con decisiones previas.

### database.md

- ✅ Documentación actualizada con schema post-auditoría (30 tablas, 33 migraciones).
- **Agregado:** `rate_limit_counters` en sección Sistema (propósito, PK compuesta, ventana fija, upsert atómico, patrón server-only).
- **Agregado:** sección "Enums (20)" — `estado_caso` con 8 valores + nota de falso positivo N-3; `estado_arrepentimiento` reutilizado por ambas tablas server-only.
- **Agregado:** sección "Patrón: tabla de rol de servidor" con tabla de 4 tablas que lo siguen + contraste con `user_agreements`.
- **Agregado:** sección "Retención de datos (FKs legales)" — tabla de 4 FKs con ON DELETE (2 RESTRICT, 2 SET NULL, ninguna CASCADE).
- **Árbol de migraciones:** agregadas 4 migraciones (20260815120000…20260817140000).
- **Pendiente:** actualizado Fase 1/2 con conteos nuevos + 4 filas nuevas (rate limit, anti-contratación estudio, fix has_accepted_current, patrón server-only).
- No se documentaron objetos inexistentes (`notify_change_of_state()`, `cambios_caso`, `pagos_stripe`, `items_fijos_por_negociacion`).

### QA scripts (smoke + validate_rls)

- ✅ QA scripts extendidos para las 3 tablas nuevas y corregidos.
- **`smoke_migrations.py`:** `EXPECTED_TABLES` y `RLS_TABLES` expandidos a 30; `set_updated_at` 19→20; audit triggers 11→12; total 75/75 PASS.
- **`validate_rls.py`:** `has_accepted_current` denied test cambiado de `authenticated` a `anon` (EXECUTE ahora lo da a `authenticated`); sección "Tablas de rol de servidor (sin policies)" agregada: `run_denied_test` para `solicitudes_arrepentimiento`, `avisos_version_legal`, `solicitudes_contacto`, `rate_limit_counters` × `anon` + `authenticated` (8 checks); bonus INSERT service_role + cleanup (1 check). Total: 35/35 PASS verificado en 2 ejecuciones consecutivas.
- **`tmp/test_16_tyc_legal.sql`:** fix de idempotencia — `user_agreements` es append-only (triggers BEFORE DELETE rechazan todo), así que se deshabilitan temporalmente, se eliminan duplicados conservando la primera fila por `(user_id, document_type)`, se re-habilitan triggers, y luego cada INSERT verifica `NOT EXISTS`. Verificado idempotente en 2 ejecuciones consecutivas.
- **`smoke_migrations.py`:** 75/75 PASS; **`setup_test_env.ps1`:** 35/35 PASS; **`db lint`:** 0 errores.
