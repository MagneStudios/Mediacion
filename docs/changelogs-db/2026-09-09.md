# Changelog — Proyecto Mediación (DB)

## 2026-09-09 — Correcciones de auditoría FE + guard de INSERT en máquina de estados

> Respuesta a `docs/auditoria-desbloqueos-09-09-2026.md` (FE). Corrige 4 imprecisiones de documentación que la auditoría marcó como reales, cierra el hallazgo técnico A (la máquina de estados no corría en INSERT) y delega el hallazgo técnico B (`acordado` derivado) a Backend.

### Migración (total final: 44)

| # | Migración | Tipo |
|---|---|---|
| 44 | `20260909130000_casos_estado_insert_guard.sql` | Aditiva (cierra hallazgo técnico A) |

- `trigger_validate_caso_estado` pasa de `BEFORE UPDATE OF estado` a `BEFORE INSERT OR UPDATE OF estado`.
- `validate_caso_estado_transition()` ahora valida `TG_OP = 'INSERT'`: el estado inicial solo puede ser `nuevo` o `pendiente_suscripciones`. Las transiciones de UPDATE quedan idénticas a la versión de `20260909120000_renegociacion_reabre_caso.sql` (incluye `acordado → en_negociacion`).
- No es `SECURITY DEFINER` (solo compara OLD/NEW.estado; no lee otras tablas), coherente con el resto de la función.
- `tmp/test_18_gate_suscripciones.sql` ajustado: el caso "sin partes" se inserta en `nuevo` y pasa a `activo` vía `UPDATE` (el borde empty-set → true lo ejercita el gate C-01 en la transición, no en el INSERT).

### Correcciones de documentación (los 4 errores que FE detectó)

1. **`docs/database.md` — enums inventados.** `materia_acuerdo` y `estado_negociacion` traían valores que no existen (`custodia_hijos`, `uso_auto`, `deudas`; `negociando`/`acordada`). Corregidos a los valores reales: `materia_acuerdo = (tenencia, alimentos, bienes, otro)`, `estado_negociacion = (borrador, activa, acordada, cerrada, terminada)`.
2. **`docs/database.md` — backfill documentado como `'otro'`.** El backfill de P4 usa `materia = NULL` (modelo viejo), nunca `'otro'`. Corregido en las 4 menciones (líneas del árbol de migraciones, `negociaciones`, `materia_acuerdo`, P4 y tabla de resumen).
3. **`docs/prompts-db/implementar-cambios-schema.md` — "invitacion_ttl_horas … se lee desde el backend".** En el momento de la auditoría era falso (`apps/api` hardcodeaba 7 d en `invitation-ttl.ts`); la corrección inicial lo marcó como pendiente de BE. **Resuelto 09-09:** el backend ahora lee `configuracion.invitacion_ttl_horas` (fallback 72 h) en `invitations.repository.ts` / `invitation-ttl.ts`. Doc actualizada en consecuencia.
4. **`docs/decisiones-db/2026-09-02-c01-c02-cliente.md` — "`GET /casos/:id/invitaciones` sigue devolviendo `pago_a_cargo`".** Falso: la columna existe pero nunca se persiste ni se expone. Corregido.
5. **`docs/changelogs-db/2026-09-06.md` — "`validate_caso_estado_transition()` ya SECURITY DEFINER".** Falso: la función usa `SET search_path=''` pero no `SECURITY DEFINER`. Corregido.

### Hallazgo técnico B — `acordado` derivado: delegado a Backend

- La decisión `docs/decisiones-db/2026-09-06-acuerdos-modulares.md` §3 y el pedido `docs/pedidos-db-a-backend-acuerdos-modulares.md` §2.3 se actualizan: `estado_caso='acordado'` (todas las materias con acuerdo vigente+firmado) **lo calcula Backend** (`CasosRepository.markAcordado`); **DB no implementa trigger** para esto. Cierra la inconsistencia que FE señaló (la decisión original decía "trigger/BE lo calcula").

### Pendientes que son de Backend (nombrados, fuera de alcance DB)

- ~~`invitacion_ttl_horas` no se lee desde `configuracion` (hardcode 7 días vs 72 h del cliente).~~ **RESUELTO 09-09:** `apps/api` lee `configuracion.invitacion_ttl_horas` en `joinCase` (fallback 72 h / R-04); `invitation-ttl.ts` parametrizado. Specs ≥57 unit+4 integración verdes. Cambios: `invitation-ttl.ts(.spec.ts)`, `invitaciones.repository.ts(.spec.ts)`, `invitaciones-hardening.integration.spec.ts`.
- `pago_a_cargo` no se persiste ni se expone en `/invitaciones` (dos cambios, no uno).
- `negociaciones.estado` nunca se escribe tras la creación (motor sigue leyendo por caso).
- Versionado P5 sin consumir; `items.negociacion_id` el motor sigue leyendo por caso (flag `tsc -b` en `apps/api`).

### Confirmación impuestos (criterio del prompt 172)

- **No existe módulo de facturación en `apps/api`** (grep: sin `facturas`, `FacturaRepository`, AFIP/ARCA). Nada lee `configuracion.impuestos` hoy.
- Fuente confirmada y documentada: cuando se construya el BE de facturación (R-09), `configuracion.impuestos` (`{"AR":{"iva":21,"otros_impuestos":0}}`, seed de `20260810120000_cambios_reunion_07_08.sql`) es la fuente parametrizable por país; `planes.precio` se interpreta como neto. Sin código hasta ese módulo.

### QA

- `db lint` sobre las migraciones (incl. 44): sin errores de sintaxis.
- `db reset` + smoke + validate_rls: verificar tras correr (el entorno tiene el seed TYC de C-01 que bloquea `validate_rls` por env; no es regresión de este cambio).
- `packages/db-types` queda verde (mig 44 no cambia tipos).
