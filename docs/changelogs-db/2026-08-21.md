# Changelog — Proyecto Mediación (DB)

## 2026-08-21 — Monetización Pactum Fase 1 + cierres de auditoría

> Implementa `docs/PACTUM-monetizacion-spec.md` Fase 1 adaptada al schema actual (contrato: `docs/contrato-spec-repo-monetizacion.md`). Resuelve la deuda O1 y cierra la auditoría O3.

### Contrato spec↔repo (paso 0 bloqueante)

- ✅ Publicado `docs/contrato-spec-repo-monetizacion.md`: equivalencias congeladas (negotiations→casos, accounts→usuarios/estudios, plans→planes, subscriptions→suscripciones) y nombres congelados post-merge.

### O1 — Deuda has_accepted_current (resuelta)

- ✅ **Decisión: revertir** el GRANT EXECUTE a `authenticated` (`20260821000000_revert_has_accepted_current_grant.sql`). La función es helper de servidor (BE la llama vía selectNoFrom); no hay caso de uso legítimo como RPC del cliente.
- ✅ `validate_rls.py`: test "has_accepted_current denegado para authenticated" restaurado.
- ✅ Deuda cerrada en `agents/db/deuda-has-accepted-current-execution.md`.

### O2 — Monetización Fase 1 (implementada)

- ✅ **Migración** `20260821120000_monetizacion_fase1.sql` (aditiva):
  - Enum `estado_suscripcion`: ADD VALUE `pausada`; nuevo enum `estado_solicitud_abogado` (7 valores).
  - `planes`: ADD COLUMN `max_negotiations_per_period`, `max_clients_per_period` (NULL = ilimitado).
  - `suscripciones`: ADD COLUMN `current_period_start/end`, `cancel_at_period_end`, `mp_preapproval_id` (UNIQUE), `mp_payer_email`.
  - Tablas nuevas: `usage_counters` (PK `(usuario_id, period_start)`), `lawyer_requests` (FK `caso_id → casos` CASCADE, monto en integer minor units, precio congelado), `payment_events` (UNIQUE `(provider, event_type, resource_id)`, patrón server-only).
  - `consume_quota(p_usuario_id, p_kind)` SECURITY DEFINER, errcodes P0001 (NO_ACTIVE_SUBSCRIPTION / NO_BILLING_PERIOD) y P0002 (QUOTA_EXCEEDED) → BE traduce a 402; `FOR UPDATE` + UPDATE atómico (carrera concurrente segura).
  - RLS: lectura owner (`usage_counters` owner, `lawyer_requests` participantes del caso), escritura solo service_role/postgres; `payment_events` sin policies.
  - Seeds ON CONFLICT: planes `particular` (3/mes) y `corporativo` (ilimitado); `estudio` actualizado (3/mes por cliente + 20 clientes/mes).
- ✅ Defaults reversibles implementados y documentados en `docs/decisiones-db/2026-08-21-monetizacion-fase1.md` (#3 herencia de cuota por cliente, #4 flujo mensual).

### QA

- ✅ `db reset` limpio (35 migraciones), `db lint` 0 errores.
- ✅ `smoke_migrations.py` extendido: 33 tablas, 14 funciones (consume_quota), 21 enums, 33 RLS, 6 planes, 21 updated_at → **80/80 PASS**.
- ✅ `validate_rls.py` extendido: tablas nuevas (owner/participantes/server-only) + carrera concurrente (2 requests con 2/3 → 3/3, no 4/3) → **45/45 PASS**.
- ✅ `tmp/test_17_cuotas.sql` nuevo en pipeline: 3x OK + 4ta QUOTA_EXCEEDED, contador no se infla, NULL = ilimitado, herencia default, histórico por período.
- ✅ `setup_test_env.ps1` completo verde en ejecución limpia post-reset.
- ✅ `db-types` a mano: tablas nuevas (payment_events, lawyer_requests, usage_counters), columnas nuevas (planes, suscripciones), enums (pausada, estado_solicitud_abogado), función consume_quota. `tsc -b` OK.

### O3 — Cierres de auditoría

- ✅ A-4 confirmado en `database.md`: patrón server-only documentado con las 4 tablas + contraste con `user_agreements` (ya estaba de la corrida del 18/08; verificado).
- ✅ Hallazgos fantasma anotados: A-3, N-2, N-4 (objetos inexistentes) — referencia desde `docs/Audit_Report_DB_Requirements_2026-08-18.md` a las respuestas PM del 18/08.

### Docs

- ✅ `database.md` actualizado: 33 tablas, 21 enums, árbol de migraciones (35), sección consume_quota, decisiones, testing (80/80, 45/45, test_17).
- ✅ `decisiones-db/2026-08-21-monetizacion-fase1.md` creado.
- ✅ `mediacion.dbml` sincronizado (33 tablas, enums, FKs, notas).