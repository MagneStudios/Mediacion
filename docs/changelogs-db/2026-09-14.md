# Changelog — Proyecto Mediación (DB)

## 2026-09-14

### Ficha de contexto del caso (privacidad por parte)

> Prompt `docs/prompts-db/2026-09-14-ficha-contexto-caso.md`; decisión `docs/decisiones-db/2026-09-14-ficha-contexto-caso.md`.

- `20260914120000_caso_contexto.sql — ficha de contexto del caso (7 tablas, privacidad por parte).`

| # | Migración | Tipo |
|---|---|---|
| 46 | `20260914120000_caso_contexto.sql` | Aditiva (7 tablas + RLS + triggers + grants) |

- Ancla `caso_contexto` (`UNIQUE (caso_id)`, FK `casos(id)` ON DELETE CASCADE) + 6 hijas privadas (`contexto_integrantes`, `contexto_actividades` con `integrante_id → contexto_integrantes` ON DELETE SET NULL, `contexto_colegio`, `contexto_cronograma`, `contexto_domicilios`, `contexto_restricciones`). Todas con `set_updated_at` y grants explícitos.
- Privacidad por parte (estilo `items`/CA-02): cada hija visible/escribible solo por su `parte_id` (`(SELECT auth.uid())` como InitPlan) + `_select` con `public.is_admin()`. El padre `caso_contexto` es neutro: policy `caso_contexto_select` (cualquier parte del caso o admin); sin policies write para clientes → el ancla la crea Backend (service_role, BYPASSRLS). Ninguna parte ve la ficha de la contraparte.

| Archivo | Cambio |
|---|---|
| `supabase/migrations/20260914120000_caso_contexto.sql` | Nueva |
| `packages/db-types/src/database.types.ts` | +7 tablas: `caso_contexto`, `contexto_actividades`, `contexto_colegio`, `contexto_cronograma`, `contexto_domicilios`, `contexto_integrantes`, `contexto_restricciones` (Row/Insert/Update/Relationships; UUID→string, timestamptz/time/date→string); sin borrar nada |
| `docs/database.md` | Subsección "Ficha de contexto del caso" en módulo de casos + árbol de migraciones (46) + conteos (41 tablas / 41 RLS / 29 triggers) |
| `scripts/smoke_migrations.py` | `EXPECTED_TABLES` y `RLS_TABLES` + las 7 tablas (total 41); triggers `set_updated_at` 22 → 29 |

### Trazabilidad de moderación de lenguaje (server-only)

> Prompt `docs/prompts-db/2026-09-14-moderation-events.md`; decisión `docs/decisiones-db/2026-09-14-moderation-events.md`.

- `20260914130000_moderation_events.sql — tabla server-only de trazabilidad de moderación de lenguaje.`

| # | Migración | Tipo |
|---|---|---|
| 47 | `20260914130000_moderation_events.sql` | Aditiva (1 tabla server-only) |

- `moderation_events` sin `updated_at` (err-only con `created_at`): `usuario_id → usuarios` ON DELETE SET NULL, `caso_id → casos` ON DELETE CASCADE, `texto_detectado` (PII ofensivo completo), `accion` (`bloqueado | avisado | permitido`), `scores` JSONB. Índices `idx_moderation_usuario`, `idx_moderation_caso`.
- Patrón server-only: SELECT solo `is_admin()` vía policy (`moderation_events_admin_select`); INSERT policy dirigida a `service_role` + GRANT INSERT solo `service_role, postgres`; GRANT SELECT a `authenticated, service_role, postgres` (RLS restringe a admin). `anon` no recibe GRANT.

| Archivo | Cambio |
|---|---|
| `supabase/migrations/20260914130000_moderation_events.sql` | Nueva |
| `packages/db-types/src/database.types.ts` | +`moderation_events` Row/Insert/Update/Relationships (sin `updated_at`); sin borrar nada |
| `docs/database.md` | Subsección "Moderación de lenguaje (trazabilidad)" + árbol de migraciones (48) + conteos (42 tablas / 42 RLS) |
| `scripts/smoke_migrations.py` | `EXPECTED_TABLES` y `RLS_TABLES` + `moderation_events` (42) |

### Flag de arbitraje por materia `FEATURE_ARBITRAJE_BIENES`

> Prompt `docs/prompts-db/2026-09-14-arbitraje-bienes-flag.md`; decisión `docs/decisiones-db/2026-09-14-arbitraje-bienes-flag.md`.

| # | Migración | Tipo |
|---|---|---|
| 48 | `20260914140000_casos_arbitraje_bienes_flag.sql` | Aditiva (ADD COLUMN) |

- `20260914140000_casos_arbitraje_bienes_flag.sql — casos.arbitraje_bienes_habilitado` (flag por caso, FEATURE_ARBITRAJE_BIENES).
- `arbitraje_bienes_habilitado BOOLEAN NOT NULL DEFAULT false`, idempotente (`ADD COLUMN IF NOT EXISTS`), rollback documentado (`DROP COLUMN`).
- Granularidad por caso (la materia del caso); sin tocar RLS/grants de `casos` (ya accesibles a partes + admin). Validación "solo bienes / nunca familia" a cargo de Backend (TYC H.5/H.6; `metodo_caso` no incluye arbitraje).

| Archivo | Cambio |
|---|---|
| `supabase/migrations/20260914140000_casos_arbitraje_bienes_flag.sql` | Nueva |
| `packages/db-types/src/database.types.ts` | `casos` Row/Insert/Update con `arbitraje_bienes_habilitado: boolean` (Insert/Update opcional por DEFAULT); sin borrar nada |
| `docs/database.md` | Descripción de `casos` + árbol de migraciones |

### QA global 2026-09-14

- `supabase db reset` **48/48**; `db lint` 0 errores.
- `smoke_migrations.py` **100/100** (42 tablas / 42 RLS / 29 triggers set_updated_at).
- `validate_rls.py` 61/61 (sin degradar — RLS de contexto y moderation cubiertas por smoke; sin checks nuevos pedidos).
- `arbitraje_bienes_habilitado` presente (`type=boolean, default=false`).
- `moderation_events`: INSERT service_role ✓ / SELECT authenticated → 0 filas (RLS deny) ✓.
- `packages/db-types` typecheck **exit 0**.
- Sin cambios en `apps/api` ni `mediacion-app`.
- Nota: `20260914120000` < `20260914130000` < `20260914140000` cronológicamente, pero se aplican por orden de nombre en `db reset` (determinístico); las tres entraron en el mismo día de trabajo.