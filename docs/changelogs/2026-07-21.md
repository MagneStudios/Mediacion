# Changelog — Proyecto Mediación

## 2026-07-21

### Fase 1: DBML — Diseño del modelo de datos

**Entregable:** `schema.dbml` con 22 tablas, 18 enums y todas las relaciones.

- Disección de requerimientos: 22 entidades identificadas, 10 supuestos documentados
- Decisión `items.valor_min` / `valor_max`: tipo `text` para soportar valores numéricos y descriptivos
- Enumeración de 10 puntos "a confirmar" del documento técnico
- Presentación al PM con 5 preguntas bloqueantes

### Decisiones del PM (confirmadas)

1. **Auth:** Supabase Auth nativo (`auth.uid()`)
2. **Mediador:** Ve posiciones privadas de ambas partes (bajo confidencialidad)
3. **Mediador:** Una sola parte puede solicitar mediador
4. **CHECK XOR:** Decisión técnica — se aplica constraint en DB
5. **Webhooks:** Pendiente del backend

### Fase 2: Migraciones SQL — 7 archivos

| # | Archivo | Contenido |
|---|---------|-----------|
| 1 | `extensions` | `pgcrypto` |
| 2 | `enums` | 18 tipos (rol_usuario, estado_caso, categoria_item, etc.) |
| 3 | `tables` | 22 tablas agrupadas por módulo + GRANTS a roles Supabase |
| 4 | `constraints_indexes` | 1 CHECK XOR, 9 UNIQUE, 16 índices custom |
| 5 | `functions_triggers` | 11 funciones (updated_at, audit, sync_ronda, validación estados, RLS helpers), 45 triggers |
| 6 | `rls_policies` | RLS habilitado en 22 tablas, 41 policies por rol |
| 7 | `seed_catalog` | 4 planes (base/simple/plus/estudio) + 5 configs del sistema |

### Fixes aplicados durante testing

| Fix | Problema | Solución |
|-----|----------|----------|
| `documento` nullable | Trigger `handle_new_user` fallaba porque `documento` era NOT NULL pero no se setea en signup | Quitado NOT NULL — se completa en onboarding |
| GRANTS a roles | Rol `authenticated` no tenía permisos a nivel de tabla (RLS filtra filas, no da acceso) | `GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES TO authenticated, anon, service_role` |
| RLS en `planes` e `inversores` | Supabase Studio warn: "RLS Disabled in Public" | RLS habilitado + policies permisivas (lectura pública) |
| Puertos en `config.toml` | Conflicto con puerto 54321 | Todos los puertos cambiados a rango 5500x |

### Tests locales ejecutados (9/9 OK)

| # | Test | Qué verifica |
|---|------|-------------|
| 1 | Setup usuarios | Trigger `handle_new_user` crea registro en `usuarios` al hacer signup |
| 2 | Crear caso | Inserción + vinculación de ambas partes |
| 3 | Items privados | `text` para valor_min/valor_max funciona correctamente |
| 4 | **RLS RN-01** | Parte A solo ve sus items, NUNCA los de Parte B |
| 5 | Máquina de estados | Transiciones válidas aceptadas, inválidas rechazadas |
| 6 | CHECK XOR suscripciones | 4 escenarios: solo usuario ✅, solo estudio ✅, ambos ❌, ninguno ❌ |
| 7 | Auditoría + updated_at | Triggers registran INSERT/UPDATE con before/after |
| 8 | Sync ronda_actual | `ronda_actual` sincroniza con tabla `rondas` + unique constraint |
| 9 | Integridad FK/Unique | Parte duplicada rechazada, FK inexistente rechazada |

### Infraestructura local

- Supabase CLI corriendo en Docker
- Puertos: API 55001, DB 55002, Studio 55003, SMTP 55004
- `.env.example` creado con todas las variables de entorno documentadas
- `.gitignore` configurado para excluir `.env` y secrets

### Próximos pasos

1. Commitear migraciones a la rama `feature/*`
2. Fase 3: Scripts Python (seed_data.py, validate_rls.py, smoke_migrations.py)
3. Fase 4: QA/E2E (matriz de casos + suite ejecutable)
