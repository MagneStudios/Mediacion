// Re-exports from @testing-library/react-native for convenience.
// Use `await render(...)` since the v14 API is async.
export { render, screen, fireEvent, act, waitFor, within } from '@testing-library/react-native';
